BUGS :

- Header : les couleurs sont moches
- Navbar : changer le lien active quand on clique pour changer de passe[ABDELAZIZ WAS HERE (and fixed this of course hahahahahahah)]
- Body : impossible de changer le background via le css [ABDELAZIZ WAS HERE (it's not that impossible, but i don't see the 'interet', but if you have any better backgrounds you're the most welcome )]

TODO LIST :
** amélioration non demandé **
- ajouter image et description à la BDD
- bouton 'effectuer une recherche' propose d'effectué une requête et de l'affiché dans une pages
- upload d'image possible avec la méthode post dans formulaire

** important **
>>> pour le formulaire 
(Tahina)
- quel type dans le formulaire pour la durée de l'album ? DONE
- liste des albums pour le formulaire artiste DONE
- liste des instrument pour le formulaire artiste DONE
- displayAlbumsTitles() /displayInstrument() / displayMusic styles DONE
-
- envoie d'image
>>> Dans le script functions.php (ensemble de fonctions utiles pour simplifié tout):
* Brice : 
  - coder getRowByID(string $table, int $id) qui retourne un tableau associatif représentant un enregistrement d'une table passé en paramètre
  - coder function deleteRowByID(string $table,int $id) qui supprime un enregistrement d'une table passé en paramètre

* Abdelaziz :
  - coder insertRow(string $table,array $row) qui permet d'insérer un nouvelle enregistrement dans une table choisit en paramètre
  - coder updateRow(string $table,array $row) qui permet de mettre à jour un enregistrement dans une table choisit en paramètre

>>> Javascript : 
* [insérer un volontaire] :
  - bouton 'supprimer' execute la fonction deleteRowByID()
  - bouton 'modifier' execute la fonction updateRow()

IDEAS :
	[ABDELAZIZ]:
  -omg i really want to see the look on Brice's face when he'll findout we did all this already HHHHHHHHHHHHHHHHHHHHHHHHHHHHH
